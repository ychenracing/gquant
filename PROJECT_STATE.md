# 项目入口

Gquant提供冻结日线研究、连续账户回放和人工决策报告，不连接券商。

先读 `AGENTS.md`，再按工作范围读取：`README.md`（运行）、`docs/architecture.md`（模块与调用链）、`docs/strategy.md`（经济行为）、`docs/parameters.md`（字段/单位）、`docs/validation.md`（验收）。代码在 `src/gquant/`，数据在 `data/`，测试在 `tests/`。

分支、提交、PR、审阅、检查和活动运行以GitHub实时状态为准；接续已有匹配分支。源码和数据身份、完整轨迹回归及原始失败证据必须保留。任务进度记录在PR中，不把临时运行状态加入长期工程指令。
